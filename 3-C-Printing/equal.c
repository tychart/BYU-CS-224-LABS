#include <stdio.h>

int main()
{
    unsigned char u = 51;
    unsigned char c = '3';
    
    if (u == c) {
        printf("They are the same!\n");
    }
    else {
        printf("They are different!\n");
    }
    return 0;
}

